@php
    $thumbnail = $news->thumbnail ? (Str::startsWith($news->thumbnail, ['http://', 'https://', '/']) ? $news->thumbnail : asset('storage/'.$news->thumbnail)) : null;
@endphp
<article class="news-card"><div class="news-image"><a class="news-cover-link" href="{{ route('news.show', $news) }}" aria-label="{{ $news->title }}">@if($thumbnail)<img src="{{ $thumbnail }}" alt="{{ $news->title }}" loading="lazy">@else<i class="bi bi-newspaper" aria-hidden="true"></i>@endif</a></div><div><small>{{ optional($news->published_at)->format('d/m/Y') }}</small><h3><a href="{{ route('news.show',$news) }}" style="font:inherit;padding:0">{{ $news->title }}</a></h3><p>{{ Str::limit(strip_tags($news->content), 110) }}</p><a href="{{ route('news.show',$news) }}">Đọc tiếp <i class="bi bi-arrow-right" aria-hidden="true"></i></a></div></article>
