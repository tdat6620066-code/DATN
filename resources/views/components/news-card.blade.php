<article class="news-card"><div class="news-image">@if($news->thumbnail)<img src="{{ $news->thumbnail }}" alt="{{ $news->title }}" loading="lazy">@else<i class="bi bi-newspaper"></i>@endif</div><div><small>{{ optional($news->published_at)->format('d/m/Y') }}</small><h3>{{ $news->title }}</h3><p>{{ Str::limit(strip_tags($news->content), 110) }}</p><a href="#">Đọc tiếp <i class="bi bi-arrow-right"></i></a></div></article>
