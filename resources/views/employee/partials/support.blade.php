@if(auth()->user()->hasPermission('incidents.manage') && in_array($booking->status, ['PENDING_PAYMENT','CONFIRMED']))
<div class="staff-card p-4 mt-3"><h2 class="h5">Hỗ trợ đổi lịch / hủy</h2><p>Đổi sang khung giờ cùng giá còn trống. Chỉ hủy đơn chưa thanh toán; đơn đã trả tiền cần xử lý theo chính sách sự cố / hoàn tiền.</p>
@foreach($booking->bookingDetails->where('status','!=','CANCELLED') as $detail)
<form method="POST" action="{{ route('employee.bookings.reschedule',[$booking,$detail]) }}" class="border-bottom pb-3 mb-3">@csrf @method('PUT')<p>{{ $detail->court->name }} · {{ $detail->booking_date->format('d/m/Y') }} · {{ $detail->timeSlot->name }}</p><div class="row g-2"><div class="col-md-4"><label class="form-label">Ngày mới<input type="date" name="booking_date" class="form-control" min="{{ today()->toDateString() }}" required></label></div><div class="col-md-4"><label class="form-label d-block">Khung giờ mới<select name="time_slot_id" class="form-select" required>@foreach($slots as $slot)<option value="{{ $slot->id }}">{{ $slot->name }}</option>@endforeach</select></label></div><div class="col-md-4"><label class="form-label">Lý do đổi<input name="reason" class="form-control" required maxlength="1000"></label></div></div><button class="staff-button">Kiểm tra và đổi lịch</button></form>
@endforeach
@if($booking->status==='PENDING_PAYMENT')<form method="POST" action="{{ route('employee.bookings.cancel',$booking) }}">@csrf @method('PUT')<label class="form-label d-block">Lý do hủy<input name="reason" class="form-control" required maxlength="1000"></label><button class="staff-button">Hủy đơn chưa thanh toán</button></form>@endif
</div>
@endif
