@extends('layouts.admin')
@section('page_heading','Quản lý thông báo')
@section('content')
<div class="row g-3"><div class="col-lg-5"><div class="card border-0 shadow-sm p-4"><h4>Tạo thông báo</h4><form method="POST" action="{{ route('admin.announcements.store') }}">@csrf<input name="title" class="form-control mb-2" placeholder="Tiêu đề" required><textarea name="content" class="form-control mb-2" rows="5" placeholder="Nội dung" required></textarea><select name="audience" class="form-select mb-2"><option value="ALL">Tất cả</option><option value="CUSTOMER">Khách hàng</option><option value="EMPLOYEE">Nhân viên</option></select><input type="datetime-local" name="scheduled_at" class="form-control mb-3"><button class="btn btn-success">Gửi / lên lịch</button></form></div></div><div class="col-lg-7"><div class="card border-0 shadow-sm table-responsive"><table class="table mb-0"><thead><tr><th>Tiêu đề</th><th>Đối tượng</th><th>Trạng thái</th><th></th></tr></thead><tbody>@foreach($announcements as $a)<tr><td>{{ $a->title }}<br><small>{{ Str::limit($a->content,60) }}</small></td><td>{{ $a->audience }}</td><td>{{ $a->status }}</td><td>@if($a->status!=='SENT')<form method="POST" action="{{ route('admin.announcements.destroy',$a) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Xóa</button></form>@endif</td></tr>@endforeach</tbody></table></div><div class="mt-3">{{ $announcements->links() }}</div></div></div>
@endsection
