@extends('layouts.admin')
@section('page_heading','Chi tiết sân')
@section('content')<x-admin.workspace><a href="{{ route('admin.courts.index') }}">← Danh sách sân</a><x-admin.page-heading>{{ $court->name }}</x-admin.page-heading><div class="card card-body"><p>Mã: {{ $court->code }} · {{ $court->courtType?->name }} · {{ $court->status }}</p><p>{{ $court->description }}</p><p>{{ $court->address }} · {{ $court->phone }}</p><p>Giờ mở cửa: {{ $court->opening_time }} – {{ $court->closing_time }}</p><p>Tiện ích: {{ $court->amenities->pluck('name')->join(', ') }}</p><div class="d-flex flex-wrap gap-2">@foreach($court->images as $image)<img style="max-width:250px" src="{{ Storage::url($image->image) }}" alt="{{ $court->name }}">@endforeach</div><a class="btn btn-success mt-3 align-self-start" href="{{ route('admin.courts.edit',$court) }}">Chỉnh sửa sân</a></div><table class="table bg-white mt-3"><thead><tr><th>Khung giờ</th><th>Loại ngày</th><th>Giá</th><th>Trạng thái</th></tr></thead><tbody>@foreach($court->prices as $price)<tr><td>{{ $price->timeSlot?->name }}</td><td>{{ $price->day_type }}</td><td>{{ number_format($price->price) }} đ</td><td>{{ $price->status }}</td></tr>@endforeach</tbody></table></x-admin.workspace>@endsection
