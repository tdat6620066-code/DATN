@extends('layouts.admin')
@section('title', 'Nhân viên - SmashZone')
@section('page_heading', 'Nhân viên & phân quyền')
@section('content')<x-admin.workspace>
<div class="d-flex justify-content-between align-items-end mb-4"><div><x-admin.page-heading>Nhân viên & phân quyền</x-admin.page-heading><p class="text-muted mb-0">Quản lý tài khoản và phạm vi thao tác của đội ngũ.</p></div><div class="d-flex gap-2"><x-admin.filters class="d-flex gap-2"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Tên hoặc email"><button class="btn btn-dark"><i class="bi bi-search"></i></button></x-admin.filters><a class="btn btn-success" href="{{ route('admin.employees.create') }}"><i class="bi bi-person-plus me-1"></i>Thêm nhân viên</a></div></div>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Nhân viên</th><th>Role</th><th>Trạng thái</th><th>Quyền</th><th></th></tr></thead><tbody>@foreach($employees as $employee)<tr><td><strong>{{ $employee->name }}</strong><small class="d-block text-muted">{{ $employee->email }}</small></td><td>{{ $employee->role }}</td><td>{{ $employee->status }}</td><td>{{ $employee->role === 'ADMIN' ? 'Toàn quyền' : collect($employee->permissions ?? [])->map(fn ($permission) => $permissionOptions[$permission] ?? $permission)->join(', ') }}</td><td><x-admin.row-actions><a class="btn btn-sm btn-outline-secondary" href="{{ route('admin.employees.edit', $employee) }}"><i class="bi bi-pencil"></i></a></x-admin.row-actions></td></tr>@endforeach</tbody></table></div></div><div class="mt-3"><x-admin.pagination :rows="$employees" /></div>
</x-admin.workspace>@endsection
