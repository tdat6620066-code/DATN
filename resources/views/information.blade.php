@extends('layouts.app')
@section('content')<div class="container py-5"><h1>{{ $settings['site_name']??'SmashZone' }}</h1><p>{{ $settings['address']??'' }} · {{ $settings['phone']??'' }} · {{ $settings['email']??'' }}</p><a class="btn btn-success" href="{{ route('contacts.index') }}">Liên hệ hỗ trợ</a><h2 class="h4 mt-4">Câu hỏi thường gặp</h2>@foreach($faqs as $faq)<details class="card card-body mb-2"><summary>{{ $faq->question }}</summary><p style="white-space:pre-wrap">{{ $faq->answer }}</p></details>@endforeach<h2 class="h4 mt-4">Thương hiệu</h2><div class="row">@foreach($brands as $brand)<div class="col-md-4"><div class="card card-body"><h3 class="h5">{{ $brand->name }}</h3>@if($brand->image)<img style="max-height:100px;object-fit:contain" src="{{ asset('storage/'.$brand->image) }}" alt="{{ $brand->name }}">@endif<p>{{ $brand->description }}</p>@if($brand->website)<a href="{{ $brand->website }}" rel="noopener noreferrer">Website</a>@endif</div></div>@endforeach</div><h2 class="h4 mt-4">Tin tức</h2>@foreach($news as $article)<p><a href="{{ route('news.show',$article) }}">{{ $article->title }}</a></p>@endforeach{{ $news->links() }}</div>@endsection
